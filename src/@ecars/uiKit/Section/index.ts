import {SectionComponent} from '@ecars/uiKit/Section/SectionComponent';
import {SectionContainer} from '@ecars/uiKit/Section/components/SectionContainer';

export const Section = Object.assign(SectionComponent, {
  Container: SectionContainer,
});
